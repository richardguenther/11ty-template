{
  "name": "11ty-template",
  "version": "0.0.1",
  "private": false,
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "scripts": {
    "del": "rm -rf _site",
    "dev": "rm -rf _site && eleventy --serve --incremental",
    "build": "rm -rf _site && eleventy"
  }
}
